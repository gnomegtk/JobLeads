{% extends 'base.html.twig' %}

{% block title %}Statistics - Average County Tax Rate per State{% endblock %}

{% block body %}
    <h1>Statistics - Average County Tax Rate per State</h1>

    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Country</th>
                <th scope="col">State</th>
                <th scope="col">%</th>
            </tr>
        </thead>
        <tbody>
        {% for state in states %}
            <tr>
                <td>{{ state.country_name }}</td>
                <td>{{ state.name }}</td>
                <td>{{ state.average * 100 }}%</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
