{% extends 'base.html.twig' %}

{% block title %}Statistics - Average Taxrate per Country{% endblock %}

{% block body %}
    <h1>Statistics - Average Taxrate per Country</h1>

    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Country</th>
                <th scope="col">%</th>
            </tr>
        </thead>
        <tbody>
        {% for country in countries %}
            <tr>
                <td>{{ country.name }}</td>
                <td>{{ country.average * 100 }}%</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
