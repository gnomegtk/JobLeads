{% extends 'base.html.twig' %}

{% block title %}Statistics - Collected Taxes per Country{% endblock %}

{% block body %}
    <h1>Statistics - Collected Taxes per Country</h1>

    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Country</th>
                <th scope="col">Total</th>
            </tr>
        </thead>
        <tbody>
        {% for country in countries %}
            <tr>
                <td>{{ country.name }}</td>
                <td>{{ country.total }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
